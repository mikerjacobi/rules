package com.doit.do_it;
import com.doit.do_it.Idea;

global Response resp = new Response();


rule "do coinflips"
    when
        Idea(successLiklihood > 0.5f)
    then
        resp.setDoIt(true);
end